[build]
  publish = "public"
  command = "npm run setup && npm run build:graphql && mkdir public && echo hello > public/index.html"
  functions = "./src/graphql/functions-dist"

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
